const RAT = require('../../constants/Common').ROUTE_ACCESS_TYPE;

module.exports = {
    // Catalog
  //  'GET /dict/measure-unit': {authType: RAT.private, middleware: ['MeasureUnitCatalog.getCatalog']},

};
